<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Remote Servers Configuration
    |--------------------------------------------------------------------------
    |
    | Configure the remote servers that can be used for database and storage
    | synchronization. The key is the server host/name and the value is the
    | SSH user to connect with.
    |
    */
    'remote_servers' => [
        // Add more servers as needed:
        // 'staging.example.com' => 'staging_user',
        // 'production.example.com' => 'prod_user',
    ],
];
